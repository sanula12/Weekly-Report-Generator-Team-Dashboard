spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/report_app
    username: postgres
    password: "Kumuisha#123"
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: disabled
      password: disabled
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

app:
  jwt:
    secret: your-super-secret-key-minimum-32-chars-long-1234
    expiration: 86400000
  openai:
    api-key: ${OPENAI_API_KEY:}

server:
  port: 8080